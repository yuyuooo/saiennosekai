<% if crop_folder.favorited_by?(current_user) %>
  <%= link_to crop_folder_favorites_path(crop_folder), method: :delete, class: "ml-5 link", remote: true do %>
    ♥<%= crop_folder.favorites.count %> いいね
  <% end %>
<% else %>
  <%= link_to crop_folder_favorites_path(crop_folder), method: :post, class: "ml-5 link", remote: true do %>
    ♡<%= crop_folder.favorites.count %> いいね
  <% end %>
<% end %>